1.1 - 
"Prints a String and then terminate the line. 
This method behaves as though it invokes print(String) and then println()."
The println(String x) method actually combines the use of print() & println().
1.2 -
The object representing the standard input stream is 'in',
which is a PrintStream type.
1.3 -

1.4 -
A method overload is when multiple methods of the same name are created
with differing parameters.
1.5 -
nanoTime() cannot be used as a wall-clock time.
1.6 - 
java.util
1.7 - 
the Math.sin() method uses radians for the parameter.

